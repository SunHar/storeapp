import storeapplication.*


class BootStrap {

    def init = { servletContext ->

/*  User user = new User(name:'Sunil', email:'Sunil@firstProject.com', webpage:'http://www.ourfirstproject.com')
  User otherUser = new User(name:'Harini', email:'harini@firstProject.com', webpage:'http://www.ourfirstproject.com')
  if (!user.save()){
    log.error "Could not save user!!"
    log.error "${user.errors}"
  }
  if (!otherUser.save()){
    log.error "Could not save otherUser!!"
  }

  Feedback feedback = new Feedback(title:'Book', feedback:'This book is awesome', user:user)
  feedback.save()

  Comment comment = new Comment(comment:'Yuppiee', user:otherUser)
  comment.feedback = feedback
  comment.save(); 
*/
}

  def destroy = {
  }

}
